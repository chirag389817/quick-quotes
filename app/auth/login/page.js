import React from "react";
import LoginForm from "./LoginForm";

export const metadata = {
    title: "QQ - Login",
    description: "Login to quick quotes"
};

function page() {
    return <LoginForm />;
}

export default page;
